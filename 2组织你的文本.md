要见树木更见森林

## 2.1 文字与符号
### 2.1.1 字斟句酌
#### 2.1.1.1 从字母表到单词
UTF-8编码的直接输入

字符映射表

字体的支持

使用命令输入特殊字符

`babel` 宏包

`inputenc` 宏包

连字 使用 `\/` 或则空的分组  `{}` 来取消连字

`fontspec` 宏包

#### 2.1.1.2 正确使用标点
引号在LaTeX中使用单引号来表示，双引号使用两次单引号来表示

`\,` 可以产生很小的间隔

`\-` 的使用，一个表示连字符，两个en dash，三个em dash即破折号

西文省略号使用 `\dots` 和 `\ldots`表示

有特别作用的符号可以利用 `\` 来转义

全角标点

`\punctstyle` `xeCJK` 宏包

#### 2.1.1.3 看不见的字符---空格与换行
带子

西文标点后应加空格

幻影 

`\par`命令 分段

`\\` 换行

#### 2.1.2 特殊符号
`\symbol` 命令

### 2.1.3 字体
#### 2.1.3.1 字体的坐标
字号、字体编码、字体族、字体形状、字体系列

带参数的命令

字体声明

`ctex` 宏包

新字体选择方案 `NFSS`

#### 2.1.3.2 使用更多字体
`txfonts` 宏包

`times` 宏包

`mathptmx` 宏包

`fontspec` 宏包

`fc-list` 命令行命令

#### 2.1.3.3 强调文字
`\emph` 命令

`\em`声明

`ulem` 宏包

`CJKfntef` 宏包

从METAFONT到OpenType

### 2.1.4 字号和行距
`setspace` 宏包

### 2.1.5 水平间距和盒子
#### 2.1.5.1 水平间距
负距离

谓胶

胶皮长度

#### 2.1.5.2 盒子

## 2.2 段落和文本环境

### 2.2.1 正文段落
段落首行缩进

段落之间的垂直距离

对齐方式

断词

`microtype` 宏包

段落宽度

悬挂缩进

`lettrine` 宏包

`shapepar` 宏包

### 2.2.2 文本环境
引用环境 `quote` 环境和 `quotation` 环境

诗歌韵文 `verse` 环境

摘要环境 `abstract` 环境

### 2.2.3 列表环境
#### 2.2.3.1 基本列表环境
编号的 `enumerate` 环境、不编号的 `itemize` 环境、使用关键字的 `description` 环境 ``

环境内使用 `\item` 命令开始一个列表项

嵌套使用

#### 2.2.3.2 计数器和编号
控制列表编号 重定义命令

#### 2.2.3.3 定制列表环境

### 2.2.4 定理类环境
`\newtheorem`

### 2.2.5 抄录和代码环境
#### 2.2.5.1 抄录命令与环境

`\verb` 命令

`verbatim` 环境

#### 2.2.5.2 程序代码与listings
`listings` 宏包

### 2.2.6 tabbing环境

### 2.2.7 脚注和边注
`\footnote` 命令

`\marginpar` 命令

### 2.2.8 垂直间距和垂直盒子

## 2.3 文档的结构层次
### 2.3.1 标题和标题页
声明标题内容 实际输出标题内容
`\title` `\author` `\date`
`\and`
`\maketitle`

`titlepage` 环境
`titling` 宏包

### 2.3.2 划分章节
`\part`
`\chapter`
`\section`
`\subsection`
`\subsubsection`
`\paragraph`
`\subparagraph`

计数器 `secnumdepth` `tocdepth` `chapter` `section` 

`\appendix`
`appdendix` 宏包

`\fronmatter` `\mainmatter` `\backmatter`

### 2.3.3 多文件编译
`\include`
`\clearpage` `\cleardoublepage`
`\includeonly`
`\input` `\endinput`

`syntonly` 宏包

### 2.3.4 定制章节格式
`ctex` `\CTEXsetup`

`titlesec`

## 2.4 文档类和整体格式设计
### 2.4.1 基本文档类和ctex文档类
`ltxdoc` `moderncv`

### 2.4.2 页面尺寸和geometry

### 2.4.3 页面格式和fancyhdr

### 2.4.4 分栏控制和mutilcol

### 2.4.5 定义命令与环境
`\newcommand`
`\renewcommand`
`\providecommand`
`\newenvironment`
`\renewenvironment`